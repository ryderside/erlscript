%% 编译配置
{erl_opts, [
    debug_info,
    warnings_as_errors, 
    warn_export_all,
    {d, 'DEBUG'}, %% TODO 开发用, 发布时关闭
    {d, 'TEST'}
]}.

%% deps
{deps_dir, ["deps"]}.
{deps, [
]}.

%% xref 检查
{xref_warnings, true}.

{xref_checks, [
    undefined_function_calls
]}.
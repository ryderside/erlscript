-script([MapId]).

S = map:get_state();

if (S == init) {
    List = map_cfg:get_monster_list(MapId),
    map:add_monster(List)
}
else if(S == run) {
    map:loop_ai();
}
else if(S == finish) {
    map:finish()
}
